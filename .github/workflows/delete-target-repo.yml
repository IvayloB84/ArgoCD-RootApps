name: Post-Merge Target Repository Deletion

on:
  push:
    branches:
      - main
    # Triggers instantly whenever a commit on main removes/alters files in this folder
    paths:
      - 'argocd-apps/**'

jobs:
  delete-external-repo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code to Inspect Commit History
        uses: actions/checkout@v4
        with:
          fetch-depth: 2 # Fetches current commit and parent to compare changes

      - name: Extract Repository Name from Deleted Folder
        id: parse
        run: |
          # Finds which subfolder name under argocd-apps/ vanished or changed in this merge commit
          DELETED_FOLDER=$(git diff-tree --no-commit-id --name-only -r HEAD | grep '^argocd-apps/' | awk -F'/' '{print $2}' | uniq | head -n 1)
          
          if [ -z "$DELETED_FOLDER" ]; then
            echo "No specific folder modification detected in the argocd-apps block. Aborting."
            exit 0
          fi
          
          echo "Extracted target repository name: $DELETED_FOLDER"
          echo "target_repo=$DELETED_FOLDER" >> $GITHUB_OUTPUT

      - name: Permanently Delete Target Repository via REST API
        if: steps.parse.outputs.target_repo != ''
        env:
          ACCOUNT_TOKEN: ${{ secrets.REPO_DELETE_PAT }}
        run: |
          echo "Folder change detected. Purging standalone repository: ${{ steps.parse.outputs.target_repo }}"
          
          # Executes a direct DELETE call to the strict ://github.com endpoint with closing parenthesis
          RESPONSE=$(curl -s -o response.json -w "%{http_code}" \
            -X DELETE \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $ACCOUNT_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository_owner }}/${{ steps.parse.outputs.target_repo }}")

          if [ "$RESPONSE" -ne 204 ]; then
            echo "API Execution Failed with Status Code: $RESPONSE"
            cat response.json
            exit 1
          fi
          echo "Success! Repository has been permanently removed."
