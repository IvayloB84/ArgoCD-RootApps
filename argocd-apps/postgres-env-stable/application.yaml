apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "postgres-env-stable"
  # LOCK IN: Kept in argocd namespace so your parent app-of-apps tracks it
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/IvayloB84/postgres-env-stable.git"
    targetRevision: "infra/add-postgres-env-stable"
    path: k8s
  destination:
    server: 'https://kubernetes.default.svc'
    # LANDING ZONE: Where your actual app code pod runs inside the cluster
    namespace: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true