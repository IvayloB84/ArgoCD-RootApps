apiVersion: apps/v1
kind: Deployment
metadata:
  name: stratos-routing-proxy
  labels:
    app: stratos-routing-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stratos-routing-proxy
  template:
    metadata:
      labels:
        app: stratos-routing-proxy
    spec:
      containers:
        - name: app-container
          image: "eclipse-temurin:21-jre"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: stratos-routing-proxy
  labels:
    app: stratos-routing-proxy
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: stratos-routing-proxy