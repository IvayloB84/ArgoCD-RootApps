apiVersion: v1
kind: Service
metadata:
  name: "spa-delivery-engine"
  labels:
    app: "spa-delivery-engine"
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: "spa-delivery-engine"
