apiVersion: v1
kind: Service
metadata:
  name: "pulsar-java-service"
  labels:
    app: "pulsar-java-service"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: "pulsar-java-service"
