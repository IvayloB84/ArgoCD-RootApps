apiVersion: apps/v1
kind: Deployment
metadata:
  name: alpha-helix-nginx
  labels:
    app: alpha-helix-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alpha-helix-nginx
  template:
    metadata:
      labels:
        app: alpha-helix-nginx
    spec:
      containers:
        - name: app-container
          image: "nginx:stable-alpine-slim"
          ports:
            - containerPort: 80
          command: sh,-c
          args: echo Runtime container initialized. Waiting for code deployment... && sleep infinity
          env:
            - name: SERVICE_TYPE
              value: "nginx"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: ""
            - name: POSTGRES_DB
              value: "app_db"
            - name: REDIS_PASSWORD
              value: ""
            - name: JAVA_OPTS
              value: ""
            - name: APP_ENV
              value: ""
            - name: NGINX_ROUTING_PROFILE
              value: "standard-spa"

---
apiVersion: v1
kind: Service
metadata:
  name: alpha-helix-nginx
  labels:
    app: alpha-helix-nginx
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: alpha-helix-nginx