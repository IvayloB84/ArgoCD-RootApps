apiVersion: v1
kind: Service
metadata:
  name: "argo-live-service-reverse-proxy-svc"
  labels:
    app: "argo-live-service-reverse-proxy"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8181
      protocol: TCP
      name: http
  selector:
    app: "argo-live-service-reverse-proxy"