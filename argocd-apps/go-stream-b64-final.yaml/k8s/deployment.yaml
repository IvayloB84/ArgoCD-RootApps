apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-stream-b64-final
  namespace: default
  labels:
    app: go-stream-b64-final
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-stream-b64-final
  template:
    metadata:
      labels:
        app: go-stream-b64-final
    spec:
      containers:
        - name: app-container
          image: redis:alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: go-stream-b64-final
  namespace: default
  labels:
    app: go-stream-b64-final
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: go-stream-b64-final