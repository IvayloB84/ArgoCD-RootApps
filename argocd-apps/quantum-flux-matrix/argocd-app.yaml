apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quantum-flux-matrix
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/IvayloB84/quantum-flux-matrix.git'
    targetRevision: HEAD
    path: '.'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true