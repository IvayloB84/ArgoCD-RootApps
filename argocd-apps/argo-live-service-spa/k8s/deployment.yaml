apiVersion: apps/v1
kind: Deployment
metadata:
  name: "argo-live-service-spa"
  labels:
    app: "argo-live-service-spa"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "argo-live-service-spa"
  template:
    metadata:
      labels:
        app: "argo-live-service-spa"
    spec:
      securityContext:
        fsGroup: 65534
      containers:
        # 1. Web Server Engine Application Core Runtime Layer
        - name: app-container
          image: "nginx:alpine"
          ports:
            - containerPort: 9595
          env:
            - name: "NGINX_PORT"
              value: "9595"
          volumeMounts:
            - name: code-stream-volume
              mountPath: "/usr/share/nginx/html"
            - name: config-volume
              mountPath: "/etc/nginx/conf.d/default.conf"
              subPath: "default.conf"
          resources:
            requests:
              cpu: "25m"
              memory: "16Mi"
            limits:
              cpu: "150m"
              memory: "32Mi"

        # 2. Automated Git-Sync Synchronization Engine Layer
        - name: git-sync-sidecar
          image: "registry.k8s.io/git-sync/git-sync:v4.7.1"
          args:
            - "--repo=https://github.com/IvayloB84/argo-live-service-spa.git"
            - "--branch=main"
            - "--depth=1"
            - "--wait=10"
            - "--root=/tmp/git"
            - "--dest=argo-live-service-spa"
          volumeMounts:
            - name: code-stream-volume
              mountPath: /tmp/git
          securityContext:
            runAsUser: 65534
            runAsGroup: 65534
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "50m"
              memory: "32Mi"

      volumes:
        - name: code-stream-volume
          emptyDir: {}
        - name: config-volume
          configMap:
            name: "argo-live-service-spa-nginx-config"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "argo-live-service-spa-nginx-config"
data:
  default.conf: |
    server {
        listen       9595;
        listen  [::]:9595;
        server_name  localhost;

        location / {
            root /usr/share/nginx/html/argo-live-service-spa;
            autoindex on;
            try_files \$uri \$uri/ @spa;
        }

        location @spa {
            root /usr/share/nginx/html/argo-live-service-spa;
            try_files /index.html =404;
        }
    }