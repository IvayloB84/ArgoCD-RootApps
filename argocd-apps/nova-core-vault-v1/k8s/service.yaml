apiVersion: v1
kind: Service
metadata:
  name: "nova-core-vault-v1-proxy-svc"
  labels:
    app: "nova-core-vault-v1-proxy"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8349
      protocol: TCP
      name: http
  selector:
    app: "nova-core-vault-v1-proxy"