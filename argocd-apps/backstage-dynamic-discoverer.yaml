apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: backstage-scaffolded-discoverer
  namespace: argocd
spec:
  generators:
    # 🔍 WATCHES A FILE: ArgoCD watches a single roster file for changes
    - git:
        repoURL: 'https://github.com/IvayloB84/ArgoCD-RootApps'
        revision: main
        files:
          - path: "argocd-apps/apps-roster.json"
  template:
    metadata:
      name: '{{name}}'
    spec:
      project: default
      source:
        repoURL: 'https://github.com/IvayloB84/{{name}}.git'
        targetRevision: main
        path: k8s
      destination:
        server: https://kubernetes.default.svc
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
