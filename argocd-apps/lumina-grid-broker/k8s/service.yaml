apiVersion: v1
kind: Service
metadata:
  name: "lumina-grid-broker"
  labels:
    app: "lumina-grid-broker"
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: "lumina-grid-broker"
