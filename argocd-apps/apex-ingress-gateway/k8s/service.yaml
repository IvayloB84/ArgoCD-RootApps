apiVersion: v1
kind: Service
metadata:
  name: "apex-ingress-gateway-proxy-svc"
  labels:
    app: "apex-ingress-gateway-proxy"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 9090
      protocol: TCP
      name: http
  selector:
    app: "apex-ingress-gateway-proxy"