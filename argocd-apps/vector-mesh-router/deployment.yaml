apiVersion: apps/v1
kind: Deployment
metadata:
  name: vector-mesh-router
  labels:
    app: vector-mesh-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vector-mesh-router
  template:
    metadata:
      labels:
        app: vector-mesh-router
    spec:
      containers:
        - name: app-container
          image: "eclipse-temurin:21-jre"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: vector-mesh-router
  labels:
    app: vector-mesh-router
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: vector-mesh-router