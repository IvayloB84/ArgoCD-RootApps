apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-complete-sync
  labels:
    app: postgres-complete-sync
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-complete-sync
  template:
    metadata:
      labels:
        app: postgres-complete-sync
    spec:
      containers:
        - name: app-container
          image: "postgres:16-alpine"
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-complete-sync
  labels:
    app: postgres-complete-sync
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres-complete-sync