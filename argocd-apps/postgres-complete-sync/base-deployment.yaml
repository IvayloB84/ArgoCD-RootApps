apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-complete-sync-deployment
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: postgres-complete-sync
  template:
    metadata:
      labels:
        app: postgres-complete-sync
    spec:
      containers:
        - name: app-container
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env: |
            - name: POSTGRES_HOST_AUTH_METHOD
              value: "trust"
          resources:
            limits:
              cpu: 250m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
