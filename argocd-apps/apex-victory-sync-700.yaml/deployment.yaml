apiVersion: apps/v1
kind: Deployment
metadata:
  name: apex-victory-sync-700
  namespace: default
  labels:
    backstage.io/kubernetes-id: apex-victory-sync-700
spec:
  replicas: 1
  selector:
    matchLabels:
      backstage.io/kubernetes-id: apex-victory-sync-700
  template:
    metadata:
      labels:
        backstage.io/kubernetes-id: apex-victory-sync-700
    spec:
      containers:
        - name: app
          image: nginx:alpine
          ports:
            - containerPort: 80