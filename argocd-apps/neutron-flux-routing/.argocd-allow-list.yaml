apiVersion: argoproj.io/v1alpha1
kind: AppSourceAllowList
metadata:
  name: "neutron-flux-routing-allow-list"
  namespace: argocd
spec:
  allow:
    - group: "apps"
      kind: "Deployment"
    - group: ""
      kind: "Service"