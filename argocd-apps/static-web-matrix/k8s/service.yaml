apiVersion: v1
kind: Service
metadata:
  name: "static-web-matrix-proxy-svc"
  labels:
    app: "static-web-matrix-proxy"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 9090
      protocol: TCP
      name: http
  selector:
    app: "static-web-matrix-proxy"