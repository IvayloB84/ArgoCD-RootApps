apiVersion: apps/v1
kind: Deployment
metadata:
  name: vortex-lifecycle-router-deployment
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vortex-lifecycle-router
  template:
    metadata:
      labels:
        app: vortex-lifecycle-router
    spec:
      containers:
        - name: app-container
          image: nginx:alpine
          ports:
            - containerPort: 80
          resources: {} 
