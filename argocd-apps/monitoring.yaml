apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-monitoring-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://prometheus-community.github.io/helm-charts'
    chart: kube-prometheus-stack
    targetRevision: 69.3.1
    helm:
      values: |
        grafana:
          persistence:
            enabled: true
            storageClassName: "local-path"
            size: 2Gi
        prometheus:
          prometheusSpec:
            retention: 1d
            # ROOTLESS ALIGNMENT: Applies root parameters to the core database container
            securityContext:
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false
              fsGroup: 0
            # FIXED CHART SYNTAX: Force-applies root privileges to the config reloader container
            prometheusConfigReloader:
              securityContext:
                runAsUser: 0
                runAsGroup: 0
                runAsNonRoot: false
            storageSpec:
              volumeClaimTemplate:
                spec:
                  storageClassName: "local-path"
                  accessModes: ["ReadWriteOnce"]
                  resources:
                    requests:
                      storage: 5Gi
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true