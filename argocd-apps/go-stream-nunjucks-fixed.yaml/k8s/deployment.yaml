apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-stream-nunjucks-fixed
  namespace: default
  labels:
    app: go-stream-nunjucks-fixed
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-stream-nunjucks-fixed
  template:
    metadata:
      labels:
        app: go-stream-nunjucks-fixed
    spec:
      containers:
        - name: app-container
          image: redis:alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: go-stream-nunjucks-fixed
  namespace: default
  labels:
    app: go-stream-nunjucks-fixed
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: go-stream-nunjucks-fixed