apiVersion: v1
kind: Service
metadata:
  name: "code-stream-volume"
  labels:
    app: "code-stream-volume"
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: "code-stream-volume"
