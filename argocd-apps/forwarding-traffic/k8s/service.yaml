apiVersion: v1
kind: Service
metadata:
  name: "forwarding-traffic"
  labels:
    app: "forwarding-traffic"
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: "forwarding-traffic"
