apiVersion: apps/v1
kind: Deployment
metadata:
  name: zenith-lifecycle-router
  labels:
    app: zenith-lifecycle-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zenith-lifecycle-router
  template:
    metadata:
      labels:
        app: zenith-lifecycle-router
    spec:
      containers:
        - name: app-container
          image: "nginx:alpine"
          ports:
            - containerPort: 80
          command: ["sh","-c"]
          args: ["echo Runtime container initialized. Waiting for code deployment... && sleep infinity"]
          resources:
            requests:
              cpu: "25m"
              memory: "16Mi"
            limits:
              cpu: "150m"
              memory: "32Mi"
          env:
            - name: SERVICE_TYPE
              value: "nginx"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: ""
            - name: POSTGRES_DB
              value: "postgres"
            - name: REDIS_PASSWORD
              value: ""
            - name: JAVA_OPTS
              value: ""
            - name: APP_ENV
              value: ""
            - name: NGINX_ROUTING_PROFILE
              value: "standard-spa"

---
apiVersion: v1
kind: Service
metadata:
  name: zenith-lifecycle-router
  labels:
    app: zenith-lifecycle-router
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: zenith-lifecycle-router