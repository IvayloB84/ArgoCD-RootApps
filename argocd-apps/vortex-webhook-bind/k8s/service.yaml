apiVersion: v1
kind: Service
metadata:
  name: "vortex-webhook-bind"
  labels:
    app: "vortex-webhook-bind"
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: "vortex-webhook-bind"
