apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quantum-sync-loop
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/IvayloB84/quantum-sync-loop.git'
    targetRevision: HEAD
    path: '.'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
