apiVersion: v1
kind: Service
metadata:
  name: "nova-core-vault-proxy-svc"
  labels:
    app: "nova-core-vault-proxy"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8957
      protocol: TCP
      name: http
  selector:
    app: "nova-core-vault-proxy"