apiVersion: v1
kind: Service
metadata:
  name: "gartner-gagic-quadrant"
  labels:
    app: "gartner-gagic-quadrant"
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: "gartner-gagic-quadrant"
