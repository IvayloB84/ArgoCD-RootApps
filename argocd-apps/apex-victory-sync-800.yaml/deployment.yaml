apiVersion: apps/v1
kind: Deployment
metadata:
  name: apex-victory-sync-800
  namespace: default
  labels:
    backstage.io/kubernetes-id: apex-victory-sync-800
spec:
  replicas: 1
  selector:
    matchLabels:
      backstage.io/kubernetes-id: apex-victory-sync-800
  template:
    metadata:
      labels:
        backstage.io/kubernetes-id: apex-victory-sync-800
    spec:
      containers:
        - name: app
          image: nginx:alpine
          ports:
            - containerPort: 80