apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-stream-no-values
  namespace: default
  labels:
    app: go-stream-no-values
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-stream-no-values
  template:
    metadata:
      labels:
        app: go-stream-no-values
    spec:
      containers:
        - name: app-container
          image: redis:alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: go-stream-no-values
  namespace: default
  labels:
    app: go-stream-no-values
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: go-stream-no-values