apiVersion: apps/v1
kind: Deployment
metadata:
  name: private-windows
  labels:
    app: private-windows
spec:
  replicas: 1
  selector:
    matchLabels:
      app: private-windows
  template:
    metadata:
      labels:
        app: private-windows
    spec:
      containers:
        - name: app-container
          image: "eclipse-temurin:21-jre"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: private-windows
  labels:
    app: private-windows
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: private-windows