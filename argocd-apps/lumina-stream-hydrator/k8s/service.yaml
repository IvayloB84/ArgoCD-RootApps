apiVersion: v1
kind: Service
metadata:
  name: "lumina-stream-hydrator"
  labels:
    app: "lumina-stream-hydrator"
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: "lumina-stream-hydrator"
