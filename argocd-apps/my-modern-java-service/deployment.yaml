apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-modern-java-service
  labels:
    app: my-modern-java-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-modern-java-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: my-modern-java-service
    spec:
      securityContext:
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: my-modern-java-service
          image: ghcr.io/ivaylob84/my-modern-java-service:latest
          env:
            - name: SERVER_PORT
              value: "8395"
          ports:
            - name: http
              containerPort: 8395
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          startupProbe:
            tcpSocket:
              port: 8395
            periodSeconds: 5
            failureThreshold: 60
          livenessProbe:
            tcpSocket:
              port: 8395
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 8395
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 60