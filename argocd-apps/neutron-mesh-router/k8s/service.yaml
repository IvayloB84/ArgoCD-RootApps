apiVersion: v1
kind: Service
metadata:
  name: "neutron-mesh-router"
  labels:
    app: "neutron-mesh-router"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: "neutron-mesh-router"
