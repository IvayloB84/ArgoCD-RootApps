apiVersion: apps/v1
kind: Deployment
metadata:
  name: vortex-packet-broker
  labels:
    app: vortex-packet-broker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vortex-packet-broker
  template:
    metadata:
      labels:
        app: vortex-packet-broker
    spec:
      containers:
        - name: app-container
          image: "postgres:16-alpine"
          ports:
            - containerPort: 80
          command: ["sh","-c"]
          args: ["echo Runtime container initialized. Waiting for code deployment... && sleep infinity"]
          resources:
            requests:
              cpu: "25m"
              memory: "16Mi"
            limits:
              cpu: "150m"
              memory: "32Mi"
          env:
            - name: SERVICE_TYPE
              value: "nginx"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: ""
            - name: POSTGRES_DB
              value: "app_db"
            - name: REDIS_PASSWORD
              value: ""
            - name: JAVA_OPTS
              value: "-Xms512m -Xmx1024m"
            - name: APP_ENV
              value: "development"
            - name: NGINX_ROUTING_PROFILE
              value: "standard-spa"

---
apiVersion: v1
kind: Service
metadata:
  name: vortex-packet-broker
  labels:
    app: vortex-packet-broker
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: vortex-packet-broker