apiVersion: apps/v1
kind: Deployment
metadata:
  name: stable-pipeline-guard
  labels:
    app: stable-pipeline-guard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stable-pipeline-guard
  template:
    metadata:
      labels:
        app: stable-pipeline-guard
    spec:
      containers:
        - name: app-container
          image: "postgres:16-alpine"
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: stable-pipeline-guard
  labels:
    app: stable-pipeline-guard
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: stable-pipeline-guard