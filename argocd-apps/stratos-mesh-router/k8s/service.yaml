apiVersion: v1
kind: Service
metadata:
  name: "stratos-mesh-router"
  labels:
    app: "stratos-mesh-router"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: "stratos-mesh-router"
