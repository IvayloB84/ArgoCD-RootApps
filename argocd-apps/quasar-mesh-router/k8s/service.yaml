apiVersion: v1
kind: Service
metadata:
  name: "quasar-mesh-router"
  labels:
    app: "quasar-mesh-router"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: "quasar-mesh-router"
