apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-modern-java-service-v5
  labels:
    app: my-modern-java-service-v5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-modern-java-service-v5
  template:
    metadata:
      labels:
        app: my-modern-java-service-v5
    spec:
      containers:
        - name: my-modern-java-service-v5
          # This image must exist in the registry before ArgoCD syncs — a CI
          # pipeline (not part of this scaffolder) needs to build and push it.
          image: ghcr.io/ivaylob84/my-modern-java-service-v5:5200d6b31922765f81e13f8a6cf1ccf7dc288b0a
          ports:
            - containerPort: 8169
          # TCP probes work regardless of whether 'actuator' was selected as
          # a dependency. If you did select actuator, swap these for
          # httpGet on /actuator/health/{liveness,readiness} instead.
          readinessProbe:
            tcpSocket:
              port: 8169
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8169
            initialDelaySeconds: 20
            periodSeconds: 15