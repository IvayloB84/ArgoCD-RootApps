# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: sqlite3
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: storage/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: storage/test.sqlite3

# SQLite3 write its data on the local filesystem, as such it requires
# persistent disks. If you are deploying to a managed service, you should
# make sure it provides disk persistence, as many don't.
#
# Similarly, if you deploy your application as a Docker container, you must
# ensure the database is located in a persisted volume.
production:
  adapter: sqlite3
  database: <%= ENV.fetch("DATABASE_URL") { "storage/production.sqlite3" } %>
  timeout: 5000                  # Keeps trying for 5 seconds before throwing a lock error
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  threadsafety: true
  pragmas:
    journal_mode: wal            # Activates WAL mode so reads & writes don't block each other
    synchronous: normal          # Optimizes disk write frequencies for containerized local-paths
    mmap_size: 134217728         # Maps a 128MB fast memory cache for high-speed indexing lookups
    journal_size_limit: 67108864 # Caps the companion log size at 64MB to preserve disk limits